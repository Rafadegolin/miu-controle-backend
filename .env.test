# Environment variables for testing

# Database de teste (NUNCA usar DB de produção!)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/miu_controle_test?schema=public"

# JWT
JWT_SECRET="test-secret-key-do-not-use-in-production-12345678"
JWT_EXPIRES_IN="1h"
REFRESH_TOKEN_SECRET="test-refresh-secret-do-not-use-in-production"
REFRESH_TOKEN_EXPIRES_IN="7d"

# Email (mock - não envia emails reais em testes)
SMTP_HOST="smtp.mailtrap.io"
SMTP_PORT=2525
SMTP_USER="test"
SMTP_PASS="test"
SMTP_FROM="noreply@test.com"

# Frontend
FRONTEND_URL="http://localhost:3000"

# MinIO (objeto storage) - teste
MINIO_ENDPOINT="localhost"
MINIO_PORT=9000
MINIO_ACCESS_KEY="minioadmin"
MINIO_SECRET_KEY="minioadmin"
MINIO_BUCKET="test-bucket"

# Node environment
NODE_ENV="test"

# Rate limiting (mais permissivo em testes)
REQUEST_TIMEOUT_MS=30000
